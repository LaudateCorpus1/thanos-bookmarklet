javascript: (async function () {
    /* Get all answers in a google search page, which have a "g" class. */
    let element = document.querySelector(".g");
    /* Remove element */
    element.remove();

    /* TODO: How to get and remove all `.g` elements? */
})();
